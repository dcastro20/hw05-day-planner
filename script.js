// add the day 

var a = moment(). format("MMMM Do");
 
$("#currentDay").append(a);

var timeStamp= moment().startOf("day").fromNow();
console.log(timeStamp);

var textArea = $("textarea");
console.log(textArea)

function hourStyle(){
for( i=0; i < textArea.length;i++);


}

